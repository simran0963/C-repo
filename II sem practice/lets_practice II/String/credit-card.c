#include<stdio.h>
#include<string.h>
#define max 100

int main()
{
	int i, num[max];
	printf("Enter the 16 digit card number: ");
	for(i=0;i<16;i++)
	{
		printf("%d ",num[i]);
	}
	for(i=0;i<16;i++)
	{
		printf("%d ",num[i]);
	}
}