#include<stdio.h>
// x=a>b ? a: b;

int main(){
	int a,b,max;
	printf("Enter a number:");
	scanf("%d %d",&a,&b);
	printf("The greater number is %d", a>b ? a :b);
// 	max=a>b ? a : b;
// 	printf("The greater number is %d",max);
}